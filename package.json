{
  "name": "groovy-notebook",
  "type": "module",
  "displayName": "Groovy Notebook",
  "description": "A basic Groovy notebook experience",
  "icon": "assets/notebook.png",
  "publisher": "pavel-kolesnikov",
  "author": {
    "name": "Pavel Kolesnikov",
    "email": "170665+pavel-kolesnikov@users.noreply.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pavel-kolesnikov/vscode-groovy-notebook.git"
  },
  "version": "2.3.0",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Notebooks"
  ],
  "keywords": [
    "Groovy",
    "Notebook",
    "REPL",
    "notebookKernelGroovy"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onNotebook:groovy-notebook"
  ],
  "contributes": {
    "configuration": {
      "title": "Groovy Notebook",
      "properties": {
        "groovyNotebook.groovyPath": {
          "type": "string",
          "default": "groovy",
          "description": "Path to the Groovy binary. Defaults to 'groovy' which assumes it's in PATH."
        },
        "groovyNotebook.javaHome": {
          "type": "string",
          "default": "",
          "description": "Path to Java installation directory. If not set, will try to use JAVA_HOME environment variable."
        },
        "groovyNotebook.compressOutputs": {
          "type": "boolean",
          "default": true,
          "description": "Compress cell outputs in the notebook file. Disable for better readability by external tools and AI agents."
        }
      }
    },
    "commands": [
      {
        "command": "groovy-notebook.createSampleNotebook",
        "title": "Create Sample Groovy Notebook"
      },
      {
        "command": "groovy-notebook.terminateKernel",
        "title": "Terminate Groovy Kernel"
      },
      {
        "command": "groovy-notebook.exportAsGroovy",
        "title": "Export as Groovy File"
      }
    ],
    "notebooks": [
      {
        "type": "groovy-notebook",
        "displayName": "Groovy Notebook",
        "selector": [
          {
            "filenamePattern": "*.groovynb"
          }
        ]
      }
    ],
    "menus": {
      "notebook/toolbar": [
        {
          "command": "groovy-notebook.terminateKernel",
          "group": "groovy-notebook",
          "when": "notebookType == groovy-notebook"
        },
        {
          "command": "groovy-notebook.exportAsGroovy",
          "group": "groovy-notebook",
          "when": "notebookType == groovy-notebook"
        }
      ]
    }
  },
  "scripts": {
    "build:groovy": "cd src/groovy && rm -rf build && mkdir build && groovyc -d build PrettyPrintHelper.groovy MacroHelper.groovy && jar cf kernel-helpers.jar -C build . && rm -rf build",
    "compile": "npm run build:groovy && tsc -b",
    "watch": "tsc -b --watch",
    "lint": "eslint src --ext ts",
    "pack": "node_modules/@vscode/vsce/vsce package --pre-release",
    "vscode:prepublish": "npm run compile",
    "dogfood": "npm run pack && code --install-extension $(ls -t groovy-notebook-*.vsix | head -1)",
    "test:ts": "mocha",
    "test:groovy": "node scripts/test-groovy.mjs",
    "test": "npm run test:ts && npm run test:groovy"
  },
  "devDependencies": {
    "@types/mocha": "^8",
    "@types/node": "^18",
    "@types/vscode": "^1",
    "@typescript-eslint/eslint-plugin": "^8.54.0",
    "@typescript-eslint/parser": "^8.54.0",
    "@vscode/vsce": "^3.7.1",
    "eslint": "^9.39.2",
    "mocha": "^10",
    "ts-node": "^10.9.2",
    "typescript": "^5"
  }
}
